# Product Spec 템플릿 v4

<!-- 지침: 이 파일이 양식의 기준. 스킬은 출력 시 HTML 주석을 모두 제거한다. 섹션 순서와 제목을 바꾸지 않는다. 문서 유형이 Enhancement면 문제, 사용자 니즈, 가설과 KR을 각 1~2줄로 줄이고 흐름도는 분기가 있을 때만 넣는다. 출처가 없는 칸은 문장을 만들지 않고 비워 두고 [Q번호]만 적는다. -->

# {프로젝트 제목}

<!-- 지침: 아래 두 줄은 스킬이 채운다. 확인 필요 건수는 본문의 ⚠️ 개수, 착수 전은 그중 우선순위 A. -->

문서 유형: {New Product | Feature | Enhancement | 실험}. 착수 전 확인 필요 {A 건수}건, 확인 필요 전체 {⚠️ 건수}건

## 용어

<!-- 지침: 문서 맨 앞. 본문에 두 번 이상 나오는 이름, 화면명, 지표명을 모은다. 코드 식별자는 도메인 팩, 코드, API 스펙, Amplitude 택소노미, 기존 문서에서 그대로 복사하고 괄호에 출처와 (기존)/(신규 제안)을 적는다. 출처가 없으면 빈칸. 본문은 이 표의 용어만 쓰고 동의어를 쓰지 않는다. -->

| 용어 | 뜻 | 코드 식별자 (출처) |
|---|---|---|
| | | |

## 메타

| 항목 | 내용 |
|---|---|
| DRI | |
| 구현 담당 | |
| Jira | |
| 기획 일정 | |
| 디자인 일정 | |
| 개발 일정 | |
| 배포 목표 | |
| 플랫폼 | |
| Confluence Page ID | |

<!-- 지침: 플랫폼은 데스크톱 웹, 모바일 웹, iOS, Android 중 해당하는 것. 실험이면 실험 Flag 행을 추가. 담당자와 일정은 추측 금지. 없으면 빈칸. -->

## 문제

<!-- 지침: 불릿 3~4개, 각 한 줄. 지금 무엇이 안 되고(현상), 그래서 사업에 무엇이 막히며(영향), 왜 지금 바꾸는지(계기). 읽는 사람은 디자이너와 개발자와 사업 담당자이므로 파일명, 코드 식별자, API 경로를 쓰지 않는다(그것은 용어 표와 구현 상세에만). 수치는 근거를 문서명이나 링크로만 적고(테이블명, 쿼리, 이벤트명은 쓰지 않음), 쿼리는 Confluence 인라인 댓글로. 근거가 없으면 "⚠️ 쿼리 첨부 필요". 서사 금지. -->

- 

## 사용자 니즈

<!-- 지침: 이해관계자별로 한 줄씩. "{누구}: {무엇을 하고 싶다}" 꼴. 최소 유저 한 줄과 회사(원티드) 한 줄. 유저 유형이 여럿이면 유형마다 한 줄. 유저 인터뷰 인용은 원문 톤 유지. 인터뷰나 설문 같은 출처가 없으면 작성자 구술로 채우고 sources.md에 구술로 기록. -->

- 

## 가설과 KR

<!-- 지침: IF, THEN, BECAUSE 각 한 줄. KR 표는 필요한 행만. Primary만 두지 않고 Secondary 또는 Guardrail을 함께. 근거 열에는 Amplitude 차트 링크나 BQ 쿼리 위치. 목표값은 작성자 입력 없이는 빈칸 + ⚠️. -->

- IF 
- THEN 
- BECAUSE 

| 구분 | 지표 | 현재 | 목표 | 측정 | 근거 |
|---|---|---|---|---|---|
| Primary | | | | | |
| Guardrail | | | | | |

<!-- 지침: 실험(A/B)이면 아래 실험 설계 표를 추가. 실험이 아니면 표 자체를 넣지 않는다. 판정 룰은 KR을 어떻게 읽을지에 대한 규칙이므로 허용. -->

| 항목 | 내용 |
|---|---|
| 배정 단위와 배분 | |
| 노출 시점 | |
| 기간 | |
| 판정 시점과 룰 | |
| 롤백 조건 | |

## 솔루션

<!-- 지침: 개조식 불릿 3~5개, 각 한 줄. 무엇을 만들어 문제를 어떻게 푸는지. 긴 문장 금지. 검토한 대안 목록은 넣지 않는다. 필요하면 제약과 참고에 "폐기된 안" 한 줄. -->

- 
- 
- 

## 구현 범위 요약

<!-- 지침: 첫 불릿 한 줄로 무엇이 바뀌는지. 비주얼 표는 Prototype, Design(Figma), 스크린샷 중 있는 것만. 변경 페이지 표의 변경 열은 "신규 생성" 또는 "기존 화면 수정. {무엇}". 메뉴 구조 표의 노출 조건은 누구에게 보이는지. 흐름도는 분기가 2개 이상일 때만, 가로형(flowchart LR)으로 그려 diagrams/{이름}.png 이미지로 넣고 코드는 넣지 않는다. 이 섹션은 표 4개까지 허용. -->

- 

| 항목 | 링크 |
|---|---|
| Design | |
| Prototype | |

<!-- 지침: 기존 화면이나 정책이 바뀌는 스펙일 때만. 신규 화면만 만들면 표 자체를 넣지 않는다. 이 표는 동작과 정책이 어떻게 달라지는지(영역 단위), 아래 변경 페이지 표는 어느 화면을 건드리는지(화면 목록). 같은 내용을 두 표에 반복하지 않는다. -->

| 영역 | 현재 | 변경 |
|---|---|---|
| | | |

| 페이지 | 변경 |
|---|---|
| | |

| depth1 | depth2 | 노출 조건 | 주요 기능 |
|---|---|---|---|
| | | | |

![사용자 흐름](diagrams/{이름}.png)

## 구현 상세

<!-- 지침: 기능 하나가 한 행. 행은 화면 단위로 묶어 정렬하고 같은 화면 안에서 진입 순서대로. 진입점은 유저가 어디서 어떤 동작으로 이 기능에 닿는지. 화면은 화면명 + (신규 생성) 또는 (기존 수정) 또는 (변경 없음) 셋 중 하나. 한 기능이 여러 화면에 걸치면 화면마다 행을 나눈다. 요구사항은 번호 불릿, 줄바꿈은 <br>, 개수 상한 없음. 8개를 넘으면 기능을 둘로 나눈다. 입력 항목은 타입, 필수 여부, 검증, 오류 문구를 요구사항 안에. 확인 사항은 같은 셀 안에 "⚠️ {내용} → {결정 주체} 확인 필요". 구현 방식(How)은 적지 않고 "→ 개발 제안 요청"으로 연다. 상태 전이나 호출 순서가 있으면 해당 행 아래에 이미지로. 이벤트 트래킹과 실험 배정은 이 표에 넣지 않고 아래 이벤트 표에. -->

| # | 진입점 | 화면 | 기능 | 요구사항 |
|---|---|---|---|---|
| 1 | | | | 1. <br>2. |

<!-- 지침: 화면 열은 화면명 + (신규 생성)/(기존 수정)/(변경 없음). 진입점이 서버 스케줄, 배치, 발송, 집계인 행은 화면 열에 "해당 없음 (발송)"처럼 적는다. -->

<!-- 지침: 트래킹 이벤트가 있을 때만. 이벤트명은 Amplitude 택소노미나 출처 문서에서 복사. 없는 이름은 만들지 않는다. -->

| 이벤트 | 시점 | property | 기존/신규 |
|---|---|---|---|
| | | | |

<!-- 지침: 신규 API가 있을 때만. 요청과 응답 항목은 출처(API 스펙, 개발 답변)가 있을 때만 적고, 없으면 "→ 개발 제안 요청". -->

| API | 용도 | 요청 | 응답 | 기존/신규 |
|---|---|---|---|---|
| | | | | |

## 제약과 참고

<!-- 지침: 기술, 법규, 일정, 타 프로젝트 충돌을 한 단락 또는 불릿. 참고 링크는 한 줄씩. 폐기된 안은 "폐기: {안}. 재사용 금지" 한 줄. -->

- 
